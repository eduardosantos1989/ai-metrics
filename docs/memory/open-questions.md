# Open questions

## Product name
Status: open
Area: naming

### Question
- Should the repository, package names, and binary continue with the spec name `ai-ledger-rs` / `ai-ledger`, or should any visible naming retain `ai-metrics`?

### Evidence
- Starting specification names the working project `ai-ledger-rs`.
- Existing Cargo.toml package name at task start was `ai-metrics`.

### Next step
- Use spec-aligned crate and binary names for now, while leaving this as a documented naming question.
